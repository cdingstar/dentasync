任务TODO清单：
- 消息详情（基本完成）
- 订单详情（基本部分）



##项目改进记录：

#2025-12-1
- 诊所的APP工程改名字为”DentaSync“；
- 增加联系我们；
- 我的 这个页面 去掉 联系我们，把 “关于我们” 这个入口调整到我们的顶部的右上角，增加一个“联系我们”的按钮。点击后再弹出 关于我们 这个页面；
- 首页 “已完成订单”、“待处理订单” 这个2个块点击后 需要跳转到对应的 订单这个 tab里面的对应的 按钮 ；
- 产品库 筛选条件 这个对话框的 顶部的下拉框整个删除掉；
- 首页点击 “我的消息” 需要 跳转到 “消息” 这个tab里面；
- 产品库 里 增加 “种植”这个类型和对应的产品；
- 选择种植 类产品后 弹出 对应的种植产品的参数选择窗；
- 增加种植系统的参数到下单页面；
- 已发货 点击后 跳转到 订单tab里面对应的按钮；
- 产品库 页面 顶部的搜索栏里面 ”搜索“ 2个文字按钮 的位置只需要显示一个图标；
- 患者档案 去掉搜索EDIT 改到右上角；

#2025-11-27
- 统一开发与预览端口为3003，确保本地预览可用；
- 增强入口挂载逻辑，增加DOMContentLoaded兜底与错误提示；
- 修复QuickOrder模块越界useEffect引用未定义变量导致崩溃；
- 重建牙位选择组件ToothSel并替换旧实现，新增受限选择与SVG牙位UI；
- 颜色设定的牙位选择限制为产品信息已选牙位集合，并默认全选该集合；
- 支持产品名称改选走产品库，暂存改动并在“立即下单”时统一应用；
- 一键下单底部操作区新增“保存”按钮，与“下单”“重置”同排；
- 开始拆分QuickOrder为多模块组件，新增BaseInfoSection与PatientInfoSection，并引入QuickOrder.jsx入口；
- 工作台顶部仅显示诊所名称，隐藏组织下拉与右侧搜索；
- 完全移除Header组织下拉与箭头相关代码；
- “我的”页面隐藏顶部搜索图标；
- 订单页顶部Tab删除“制作中”，将“待确认”改为“待处理”；
- 订单卡片移除紧急状态展示与“查看物流”按钮，状态不再显示百分比；
- 订单卡片信息行调整为“患者/医生（诊所）”，责任单位行改为“预期时间：日期（生产单位）”，移除多余文案；
- “待下单”Tab的操作按钮改为“去下单”“删除”；
- 登录页采用与Header一致的渐变底色与半透明卡片风格，提升视觉一致性；
- 所有对话框页面顶部“返回”统一使用主按钮样式，与“确定”一致；

- Header 搜索统一弹窗组件按页面自动提示（消息中心/订单管理/产品库），使用全局事件触发搜索；
- 点击 Header 搜索先关闭当前页面所有对话框/浮层，增加 `requestCloseDialogs` 事件监听；
- 消息中心顶栏改为全局 Header 标题“消息中心”，移除页面内部“消息中心/全部已读”行；
- 消息中心列表重构为订单/联系人线程，支持未读角标与时间显示，并支持按订单ID/联系人/标题搜索；
- 新增联系人聊天页 ContactChat（不支持语音），支持文本与文件消息、时间与已读/未读状态；
- 优化订单聊天页 OrderChat，混排系统通知（时间变更、生产开始）、图片消息、文本消息，统一状态展示；
- 统一聊天页与消息列表头像为 32x32 正方形、圆角 8，避免全局 `.avatar` 冲突（改用 `.chat-avatar` 专属类）；
- 聊天布局统一两行：第一行“发信人 + 时间”，第二行正文；我方消息整体右侧对齐，气泡使用亮色背景；
- 订单页面搜索占位文案改为“患者/医生/订单号”，过滤新增按 `doctorName`；
- 修复 Messages.jsx JSX 返回结构错误（Fragment 包裹多个根节点导致的解析报错）。


#2025-11-25
- 新增组件 DentaSyncApp/src/components/ToothSel.jsx ，采用 SVG 牙位图，实现选择、全口、上颌、下颌等功能，返回所选牙位数组。
- 新增样式 DentaSyncApp/src/components/ToothSel.css ，适配 App 的 overlay 结构与按钮风格。
- 产品名称改选走产品库，并在“立即下单”时统一应用。
- 颜色设定的牙位选择只允许选择“产品信息”中已选择牙位的子集。
- 颜色设定的“牙位选择”弹框默认高亮产品信息里已选的全部牙位。
- 工作台顶部仅显示诊所名称，隐藏切换下拉与右侧搜索。
- 未完成订单文案保持为“未完成订单”（现已为该文案，无需变更）。
- 移除患者紧急状态展示（“加急/普通/特急”不再显示）。




###
TOM HUANG的清单：
1. 需要提供：所有的产品清单+图片、设计方案说明+图片、附件清单；
2. 种植参数 列表；
